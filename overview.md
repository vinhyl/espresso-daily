# ESPRESSO DAILY — 视觉设计规范（Design System）

> 现代精致亮调工业风 · 咖啡。配套说明见 [README.md](README.md) 的「视觉设计规范」一节。
> 本文件为设计系统的深度参考，是 `assets/style.css` 的权威说明。

## 1. 设计语言

打破传统工业风的沉重与硬朗，用「**微水泥底色 + 庄园绿点缀 + 触感材质**」组合，打造兼具自然生机与现代高级感的精品咖啡网页。

- 底座：带噪点的微水泥暖白铺底；
- 骨架：1px 哑光铸铁黑细线划分几何区域，**弃用重阴影**，保持扁平工业切割感；
- 点缀：在按键与关键信息处集中施加庄园绿，面积越集中越克制；
- 浮层：顶部磨砂玻璃，带来现代通透感。

## 2. 色彩体系（70 / 20 / 10）

| 角色 | Token | 值 | 用途 |
|---|---|---|---|
| 主背景 | `--bg-main` | `#F4F3EF` 微水泥暖白 | 全站底色（70%） |
| 卡片 / 模块 | `--bg-surface` | `#E6E4DF` 灰泥浅灰 | 卡片、侧栏、规格面板（20%） |
| 主文字 / 线框 | `--text-primary` | `#2B2A28` 哑光铸铁黑 | 标题、正文、1px 结构线框 |
| 副文字 / 线 | `--text-secondary` | `#8C8881` 石墨中灰 | 副标题、分割线、非激活态（规范原值，约 3:1） |
| 弱文字 | `--text-muted` | `#67615A` | 小号元信息 / 面包屑（AA 安全变体，主底≈5.5 / 卡底≈4.8 :1） |
| 品牌点缀 | `--accent` | `#8A9A86` 庄园绿 | CTA 按键、评分 gauge、选中态、hover 填充 |
| 悬停加深 | `--accent-hover` | `#73836F` | 按钮 hover 压暗（暗字约 3.6:1，已确认保留） |
| 高亮微酸绿 | `--accent-bright` | `#AACC00` | 仅用于小色块 / 点（信号点、强调块），不作大面积填充 |
| 辅助填充 | `--accent-soft` | `rgba(138,154,134,0.14)` | hover 浅染背景 |
| 辅助线 | `--accent-line` | `rgba(138,154,134,0.5)` | 链接下划线、绿系描边 |
| 发丝分隔线 | `--line` | `rgba(43,42,40,0.12)` | 区块 / 行分隔 |
| 铸铁黑边框 | `--iron` | `rgba(43,42,40,0.55)` | 卡片 1px 硬朗线框 |
| 铸铁黑弱框 | `--iron-soft` | `rgba(43,42,40,0.22)` | 内部次级描边 |

## 3. 材质与触感

### 3.1 细微 Noise 噪点纹理
- 应用位置：主背景 `#F4F3EF` 与卡片背景 `#E6E4DF`。
- 实现：`--noise` 为去饱和 SVG `feTurbulence` 噪点（约 4.5% 透明度），以 `background-image` 叠加，`background-size: 180px 180px` 平铺。
- 原理：模拟清水混凝土 / 微水泥的细腻微颗粒，赋予背景高档艺术纸般的柔和质感。

### 3.2 半透明磨砂玻璃（Backdrop Blur）
- 应用位置：顶部悬浮导航 `.masthead`。
- 实现：`background: rgba(244,243,239,0.72)` + `backdrop-filter: blur(12px) saturate(1.15)`，`border-bottom: 1px solid var(--line)`。
- 原理：底层绿色按钮 / 咖啡摄影滑过时产生精致模糊色彩扩散，注入现代 UI 的灵动与通透。

## 4. 字体

| 用途 | 字体 | 备注 |
|---|---|---|
| 标题 / 大数字 | **Space Grotesk** | 工业无衬线，`--display` |
| 正文 | **IBM Plex Sans** | `--sans` |
| 数据 / 规格 / 标签 | **IBM Plex Mono** | `--mono` |
| 中文 | **Noto Sans SC** | 各字体栈均后置回退 |

## 5. 布局与组件

| 模块 | 做法 |
|---|---|
| 顶部信号条 | 3px 庄园绿 `#8A9A86` 横条 `.topbar` |
| 悬浮导航 | 磨砂玻璃 masthead：wordmark + mono 标语 + 总览统计 + `mast-actions` 按钮组（查看归档 = 透明描边 `btn-outline`；RSS / 语言切换按钮已注释隐藏，样式预留） |
| 首页 Hero | 非对称网格：左侧超大标题 + kicker（精选计数，不重复日期）+ 一句定位；右侧规格统计卡 |
| 今日精选 | 非对称：左主文 + 右**规格面板**（评分 + 0–100 gauge、来源）；标题旁**无** score-pill（评分只此一处）；规格卡含 ISSUE 徽章 + 3 项统计 + 右下「最新日报」CTA 按钮 |
| 今日资讯流入口 | `today-strip` 卡片：左精简日期点缀、中条目列表、右「查看 →」；条目含深度解读时标题旁带 `dive-badge` 徽标 |
| 近期日报 | 均匀双列 `past-grid`（每行两张卡片，响应式降为 1 列）；卡片顶部显示该日**总标题**（`.dcard-headline`，display 字体 + 虚线分隔，最多 2 行截断）；条目含深度解读时带 `dive-badge` 徽标 |
| 归档页 | 规格表发丝行（日 / 月日 / 星期 / 标题 / 篇数 / 链接），**不**展示深度解读徽标；标题 = 该日**总标题**（`kind: headline` sidecar），缺失日期回退当日最高分条目标题 |
| 单日页 | 面包屑 + 工业搜索框（带 `aria-label`）+ 左侧正文 / 右侧 sticky TOC；TOC 条目含深度解读时带 `dive-badge` 徽标 |
| 页脚 | mono 管线说明，工业标签感 |

> **去重原则**：近距离内同一信息只出现一次。日期仅保留 masthead 一处；评分仅保留规格面板一处。

### 按钮体系（三级 + 预留位）

| 层级 | 类 | 样式 | 用法 |
|---|---|---|---|
| 主 CTA | `.btn-ghost` | 庄园绿填充 + 铸铁黑字 | 页面级主行动，如 hero「最新日报」，全站唯一 |
| 导航次级 | `.btn-outline` | 透明底 + 1px 铁灰描边，hover 绿染 | 顶部工具/导航按钮，如「查看归档」（36px 高） |
| 轻量入口 | 文本链接（mono + 绿箭头） | 无边框 | 列表内入口，如 today-strip「进入今日日报」、dcard「阅读全文 →」 |
| 预留 | `.btn-icon` / `.lang-switch` | 36px 图标按钮 / 中·EN 分段文本 | RSS 订阅、语言切换；模板中已注释隐藏（样式备好），待 feed.xml 生成与英文版就绪后取消注释启用 |

> 顶部 `mast-actions` 按钮组统一 36px 高、垂直居中；顺序为「归档 → （RSS → 语言）」。

## 6. 可访问性（实测）

| 项目 | 结论 |
|---|---|
| 小号弱文字 | `--text-muted #67615A`，主底 / 卡底均 AA（≈5.5 / 4.8 :1） |
| CTA 按钮 | 默认庄园绿 `#8A9A86` + 铸铁黑字 ≈4.8:1（AA）；hover 压暗 `#73836F`（暗字≈3.6:1，微反馈 + 1px 黑边框，已确认保留） |
| 副标题 | `#8C8881` 按规范原值（约 3:1），如需严格合规可加深 |
| 交互状态 | 全站含 `:active` 按下态 + `:focus-visible` 焦点环，尊重 `prefers-reduced-motion` |
| 表单 | 搜索框有 `aria-label`；`<main>` landmark 完整 |

## 7. 关于标签（重要约束）

当前为**观察期**，`config.example.toml` 的 `[ui].show_tags = false`，**标签页与标签 chips 不展示**（构建 `tags: 0`）。本规范中所有标签相关样式均已预留但默认不渲染；改回 `true` 才会显示，**勿擅自开启**。

## 8. 产物

- `assets/style.css` — 庄园绿 · 微水泥亮调工业风系统（噪点 + 磨砂玻璃 + 1px 铸铁黑线框，变量驱动）
- `assets/app.js` — 搜索 / 标签筛选（筛选 UI 仅在 `show_tags=true` 时启用）
- `src/templates/` — `layout.html` / `index.html` / `day.html` / `archive.html` / `tag.html` / `macros.html`
- `public/` — 构建产物（首页 / 各日期页 / 归档页，全部应用本规范）
