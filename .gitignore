# 环境与密钥（绝不提交）
.env
config.toml

# 工作区本地数据（工作记忆/备份，不提交公开仓库）
.workbuddy/

# Git Bash 下误创建的 Windows 保留名垃圾文件（dir > nul 重定向产物）
nul

# Python
__pycache__/
*.pyc
.venv/
venv/

# 运行时产物（抓取失败报告等，按需本地查看，不提交）
reports/

# 阶段三临时产物：知识库补丁提案（应用后由人工提交 knowledge/，提案本身不提交）
knowledge/patches/

# 注意：research/ 由周级 CI 提交到仓库（研究卡 + 周报 + 抽检清单），供人工复核，不忽略。

# 系统 / 编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/
