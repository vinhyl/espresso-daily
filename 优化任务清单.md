# Espresso Daily 优化任务清单

> 来源：`优化讨论.md`（内容治理方案终稿）· 整理日期 2026-08-05 · **更新 2026-08-05（阶段一已完成）**
> 总目标：从「RSS 摘要墙」（近 3 天 43 条中 Reddit 占 77%、深度解读 0 篇）转为稳定产出 **5–12 条/期** 高相关、来源多样、有取舍的「昨日精选」。
> ⚠️ 下文「行号锚点」为**实施前**快照，阶段一已改动 `config.example.toml` / `src/fetch.py` / `src/pipeline.py`，行号已偏移；以源文件为准，行号仅作历史参考。

本清单为**独立可执行文档**：含精确代码锚点（行号对应当前代码）与关键取舍理由，单独交给 AI 即可准确实施，无需再附 `优化讨论.md`（后者含大量对话噪音，仅作原始依据）。

**执行边界总原则（贯穿三阶段）**
- 沿用「每天一次 Actions + 一次构建 + 静态站」架构；不加实时层 / 多层调度。
- 全文处理与 LLM 调用是稀缺资源：仅对初筛通过 + 白名单来源做全文，避免基于截断摘要虚构细节。
- 评分用「来源身份 + 配额 + 六维证据」，**不用「来源分数 × 系数」的简单乘法**——来源只影响证据维度、配额与同分排序，不让权威但无关的内容自动高分。
- 去重/聚类先用关键词规则，规模尚小不引入向量库（不够再加 SQLite + embedding）。
- 不训练模型、不开标签 UI、不把 TOML 换 YAML。

---

### 当前代码现状（执行前先对照，避免重复定位）
- `config.example.toml`：`per_source_delay = 2.0`（:176）、`user_agent = "EspressoDaily/0.1 ..."`（:177）、`max_per_day = 30`（:51）、Barista Hustle `/blog/feed/`（:102）、Reddit `.rss`（:110）、Daily Coffee News（:94）、Sprudge（:76）、PDG（:86）。
- `src/fetch.py`：
  - `fetch_rss`（:181）：**写死 UA**（:182 `"EspressoDaily/0.1 ..."`），未读 config 的 `user_agent`。
  - `fetch_search`（:106）：写死 Mozilla UA（:107），同样未用 config。
  - `fetch_all`（:217）：源循环（:223-230）**未插入 `per_source_delay` 延时**，且失败仅 `print`，无结构化记录。



## 阶段一：先改善日报本身（采集与来源结构）　✅ 已完成（2026-08-05）

### 1.1 抓取层基础修复（扩源前置）
- [x] **修复 user_agent 未生效**（`src/fetch.py`：`fetch_rss` / `fetch_search`）：两处写死 UA 已改为统一从 `cfg["fetch"]["user_agent"]`（config.example.toml `fetch.user_agent`）取值，config 缺省时回退原字符串。影响 Barista Hustle、论坛等客户端敏感源的可用性。
- [x] **启用 per_source_delay 限流**（`config.example.toml` `fetch.per_source_delay = 2.0`）：`fetch_all` 每轮源抓取后 `time.sleep(per_source_delay)`（0 或不配置则跳过）。源一多，不延时易触发限流/封禁。
- [x] **抓取失败结构化记录**：新增 `_record_failure` / `_write_failure_report`，抓取/解析失败累积到 `reports/fetch_failures_<日期>.json`（字段：source / url / error_type / status_code / rate_limited / blocked / message），供阶段二质量报告使用。
- [x] **源级关键词二次过滤（新增，比清单更前移）**：新增 `_source_prefilter`，在 fetch 层按 `include_any` / `exclude_any` 做**不耗 LLM** 的廉价预筛（清单原只要求「配额 + 评分」，把关键词闸门前移能先砍掉明显无关/非意式内容，减少后续 LLM 调用量）。`fetch_all` 调用它后再去重。
- [x] **赞数/评论数提取（为排序信号打底）**：新增 `_extract_engagement`，Reddit `.rss` 的 `score`/`ups` 提取为 `engagement`，供 `pipeline` 选稿时按（评分↓, 互动量↓）排序。
- **边界（已守住）**：限流与 UA 是「扩源稳定性」前置项，已先做；不做这两点，1.2 加再多源也会因封禁而采不到。

### 1.2 来源修复与新增
- [x] **修复 Barista Hustle 地址**（`config.example.toml`）：`/blog/feed/` 实测返回 403，已改为根路径 `https://www.baristahustle.com/feed/`（已验证可正常返回 RSS）并启用。
  - ⚠️ **偏离原清单的合理补充**：BH 约月更，原 3 天回看窗口会永远采不到，故补 `lookback_days = 21`（仅该源）让修复真正生效。
- [x] **接入 Coffee Ad Astra**：`https://coffeeadastra.com/feed/`，`category_hint = "tech_experiment"`，配 `include_any`（Espresso/Extraction/Physics…）/`exclude_any`（pourover/v60/aeropress…）二次过滤，仅取意式相关。
- [x] **接入 CoffeeGeek**：`https://coffeegeek.com/feed/`，`category_hint = "independent_review"`，`max_per_source = 1`。
- [x] **接入 Whole Latte Love / Clive Coffee**：`wholelattelove.com/blogs/tech-tips.atom`、`clivecoffee.com/blogs/learn.atom`，均 `category_hint = "tutorial"`、`quota_group = "gear_tutorials"`、`max_per_group = 1`（两源合计每期最多 1 条）。
- **边界（已守住）**：仅加「能改变内容结构」的源；Home-Barista 公开 feed 被拦截，暂不强绕（列入后续候选）。

### 1.3 现有源治理与配额
- [x] **Reddit 降级为社区补充**：改取 `top` 流（`r/espresso/top/.rss?t=week`）+ `lookback_days = 7`；赞数作排序信号；`max_per_source = 2`；候选须命中 `include_any`（机型/参数/已尝试步骤）且不含 `exclude_any`（晒图/购买咨询/纯展示）。
- [x] **Daily Coffee News 强化意式过滤**：`include_any`（设备/浓缩/奶咖自动化/商用工作流/行业变化）/`exclude_any`（公平贸易/健康/营养/研究结论）。
- [x] **PDG / Sprudge 降为补充源**：PDG 仅收影响意式用户的豆种/烘焙/供应链（`include_any`/`exclude_any` 闸门）；Sprudge `category_hint = "industry"` + `max_per_source = 1` 作低频文化信号源。
- [x] **实现来源配额机制**：`src/pipeline.py` 新增 `LAYER_QUOTA` + `_apply_quota`，选稿前按 `category_hint` 分层计数（与清单一致）：`tech_experiment=2` / `independent_review=1` / `tutorial=1` / `industry=2` / `community=2` / `official=999`（事件触发、不硬封）。叠加每源上限（`max_per_source`）+ 同组上限（`max_per_group`，WLL+Clive 合计 1）+ 选稿前按（评分↓, 互动量↓）排序。
- [x] **`max_per_day` 30 → 12 硬上限**（`config.example.toml` `llm.max_per_day = 12`）：宁缺毋滥，资讯少的日子可以更少。

**阶段一验收（机制已就位，待真实运行数据复核）**：Reddit 占比从 77% 降至 ≤25%（靠 `max_per_source=2` + 社区层 2 条封顶保证）；每期 5–12 条（`max_per_day=12` 硬上限）；来源覆盖 ≥4 个层级且配额生效（`LAYER_QUOTA` 已对 5 个活跃层生效，manual/中文源 disabled 不计入）。
> 📌 验收需跑至少 1 期真实 `fetch+pipeline` 后看 `reports/` 实际占比；逻辑层单测（配额/预筛）已通过，端到端 `--dry-run` 已验证过滤与配额路径正确。

### 阶段一 · 实施说明（供后续阶段/新会话参考）
- **改动文件**：`src/fetch.py`（UA/限流/失败报告/预筛/互动量）、`src/pipeline.py`（LAYER_QUOTA/_apply_quota/选稿排序）、`config.example.toml`（源修复+新增+配额参数）、`SOURCES.md`（源表与配额说明）。
- **已为阶段二铺好的基础设施**：① `reports/fetch_failures_<date>.json` 失败清单（质量报告「抓取失败率/封禁源」维度直接复用）；② `_source_prefilter` 廉价关键词前筛（与阶段二 LLM 初筛不冲突，是更前置的便宜筛）；③ `_extract_engagement` 互动量信号（排序已用）。
- **阶段一尚未做（属阶段二）**：按需全文抓取（`fetch` 当前只取 RSS 摘要，无 article fetch 适配器）——这是阶段二「全文变成稀缺资源、仅对白名单+初筛通过做全文」的**前置依赖**，务必在阶段二初筛 JSON 之前补。
- **一处合理偏离**：BH `lookback_days=21`（月更源，3 天窗口采不到）。

---

## 阶段二：提升编辑判断（筛选与评分）

> 评分/筛选逻辑主要位于 `src/score.py`（LLM 阶段一初筛 + 阶段二精评与 `kind`/`references`）；落盘在 `src/pipeline.py`。改动后保持 frontmatter 机制（来源 `source`/`source_url` 单一不进参考列表，仅 `deepdive` 有 `references`）不变。

> 🔧 **实施前置（来自阶段一已建基础设施，启动本阶段先确认）**
> 1. **必须先补按需全文抓取**：`src/fetch.py` 当前只取 RSS 摘要，无 article fetch 适配器。阶段一清单 1.3 提到的「fetch 需支持按需全文」尚未做。阶段二「全文变成稀缺资源」以它为硬前置——建议新增 `fetch_full_article(source, link)`（readability/候选正文提取），在初筛 `accept` 之后再抓，避免全量抓取烧流量/被封。
> 2. **复用阶段一产物**：`reports/fetch_failures_<date>.json`（失败率/封禁源维度）；`_source_prefilter`（关键词前筛，比 LLM 初筛更前置，两者串联不冲突）；`_extract_engagement`（互动量已用于排序）。
> 3. **枚举对齐**：`score.py` 现有 `kind` 体系（news/analysis/deepdive/…）与本清单 `content_type`（expert_experiment/independent_review/news/community_case/announcement/research）是两套词表，实施时须做映射对齐，避免重复造枚举、前端/卡片字段对不上。

- [ ] **初筛 JSON（Two-Pass 第一段）**：基于标题 + RSS 摘要 + 来源身份输出结构化判断 `{accept, content_type, espresso_core, reason}`；直接拒绝非意式核心、纯展示/求助帖、广告转载、低相关研究（`content_type` ∈ expert_experiment / independent_review / news / community_case / announcement / research）。→ 与阶段一 `_source_prefilter` 串联：先关键词前筛，再过本 LLM 初筛。
- [ ] **六维可解释评分**（`src/score.py`）：意式相关性 30 / 新颖性 20 / 可操作性 20 / 证据质量 15 / 参数具体度 10 / 时效性 5；**不采用「来源分数 × 系数」**，来源身份只影响证据维度、配额与同分排序。
  - 85+ 罕见强证据 · 70–84 日报主内容 · 60–69 确有补充价值才收 · <60 不发布（`llm.min_score = 60` 已是硬门槛，本项把单分拆成六维明细 + 保留总分）。
  - 同分排序按「证据等级 > 来源多样性 > 事件是否重复」，不单看总分。
- [ ] **全文处理变成稀缺资源**：仅对白名单来源、初筛通过内容抓全文做精评（**前置依赖：上「实施前置 #1」的 `fetch_full_article`**），避免基于截断 RSS 摘要虚构细节。
- [ ] **「为什么值得读」+ 类型元信息**：卡片增加后台字段并选择性展示（独立测试 / 实验研究 / 官方公告 / 社区验证 / 一段「为什么值得读」短句），不拆频道。
- [ ] **轻量事件聚类**：标题 + 实体 + 型号 + 关键词做近 48h 聚类，同一事件只保留信息最完整一条，其余折叠为补充来源（规模小，不需要向量数据库；不够再考虑 embedding + SQLite）。
- [ ] **运行质量报告**：每次运行在 `reports/` 落 JSON/Markdown——每源候选数/入选数/拒绝原因/平均分；当期来源占比/重复率/意式核心占比；85+ 条数与深度解读数。→ 直接复用阶段一 `fetch_failures_*.json` 的失败维度，合并输出。

**阶段二验收**：60–74 分灌水条（如「咖啡渣电脑包」「每天五杯咖啡」）不再过线；同题事件不重复出现；质量报告可输出。

---

## 阶段三：沉淀差异化资产（知识库与科学雷达）

> 📌 **实施提示（来自阶段一结构）**：`src/fetch.py` 的 `fetch_all` 现已按 `source["type"]` 分发（`rss` / `search` / 其他跳过），新增 `academic` 类型只需在其循环增加 `elif s.get("type") == "academic": items.extend(fetch_academic(s))` 分支即可；建议与每日 RSS 解耦为**独立周级 CI schedule**（或在 `pipeline` 加 `--mode weekly`），不要混进每日一次 Actions。

- [ ] **每周学术雷达**：OpenAlex 检索 + Crossref 补 DOI/期刊；`src/fetch.py` 新增 `academic` 类型适配器（在 `fetch_all` 的 `type` 分发循环中加 `academic` 分支），与每日 RSS 解耦为周级任务。
- [ ] **严格检索词**：必须同时命中 `espresso / coffee puck / pressurized coffee` AND `extraction / percolation / flow / pressure / channeling / grinding / particle-size / TDS / sensory`；排除 `spent grounds / health / antioxidant / packaging / cultivation`（避免被咖啡渣/健康/种植论文淹没）。
- [ ] **研究卡固定字段**：研究对象 / 实验条件 / 核心发现 / 对意式用户的实际影响 / 不能推出什么结论 / 证据等级 / DOI；证据等级区分「同行评审全文 / 仅摘要 / 预印本 / 专家实验解读」
- [ ] **知识库补丁机制**：真正改变知识判断的论文与 Coffee Ad Astra 内容补入 `knowledge/`（事件触发，不强求日更）
- [ ] **每周人工抽检 10 条**：标注「该收 / 不该收 / 评分偏高 / 评分偏低」，积累判断标准（暂不训练模型）
- [ ] **观察 ≥14 期后**：再决定是否启用更严格的深度解读触发规则

---

## 暂不做（明确排除，避免过度工程）

- 实时层 / 多层调度（保持每日一次 Actions + 一次构建）
- 自托管 RSSHub；Home-Barista 硬绕访问限制（等稳定合规入口）
- 向量数据库（FAISS/Milvus）、LightGBM 训练、200–500 条标注
- 社区图片批量多模态分析（未来只考虑曲线/仪表/拆机图）
- 标签 UI 重开；YAML 替换 TOML（无实际收益）
- 中文源（知乎/B站）自动接入（先人工/半自动精选，第二阶段再说）

---

## 后续候选（观察后再定）

- Home-Barista 技术社区源（需稳定合规入口）
- 品牌官方 `official_watchlist` 低频监测：La Marzocco、Decent、Lelit、Profitec/ECM、Breville/Sage、Gaggia、Flair、Cafelat、Eureka、Mazzer、Mahlkönig、Niche、Zerno、Timemore、1Zpresso、Option-O（只监测新品/规格/固件/召回）
- SCA / Coffee Science Foundation（周度或事件触发）
- 读者反馈按钮、视觉分析（数据量足够后）
